# Function to check if a number is prime
def is_prime(n):
    if n < 2:
        return False
    for i in range(2, int(n**0.5) + 1):
        if n % i == 0:
            return False
    return True

# Read input range from the user
start = int(input())
end = int(input())

# Count prime numbers in the given range
count = 0
for num in range(start, end + 1):
    if is_prime(num):
        count += 1

print(count)
